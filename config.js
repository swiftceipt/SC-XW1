var config = {
    "server":
    {
        "port": 50000,
        "ipaddress" : "localhost",
    },

    "api_endpoint": "https://tenv-service.swiftceipt.com",
    "session-length": 2 * 60 * 1000
}

module.exports = config;
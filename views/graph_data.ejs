<% include partials/headers %>

<% include partials/navbar-inside %>
<p>This is the dashboard</p>
<% if (typeof sc_data == "object") { %>
                <div class="container">
                	<%= JSON.stringify(parsed) %>
                </div>
                <% } %>
<!-- graph data -->
<div id = "responseArea"></div>

<a href = "/receipts">Receipts</a> <a href = "/logout">Logout</a>
<a href = "/graph_data">Graph_data</a>